{
  "name": "heroku-root-node",
  "version": "1.0.0",
  "description": "self deployable react app",
  "engines": {
    "node": "6.11.x"
  },
  "scripts": {
    "start": "node server",
    "heroku-postbuild": "cd easy-accounts-fe/ && npm install && npm install --only=dev --no-shrinkwrap && npm run build"
  },
  "cacheDirectories": [
    "node_modules",
    "easy-accounts-fe/node_modules"
  ],
  "dependencies": {
    "express": "^4.14.1"
  },
  "license": "MIT",
  "devDependencies": {}
}